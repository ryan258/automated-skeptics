# automated_skeptic_mvp/requirements.txt
# Core dependencies
spacy>=3.4.0
nltk>=3.8
openai>=1.0.0
requests>=2.28.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: For enhanced NLP (download separately)
# python -m spacy download en_core_web_sm

# Note: sqlite3 and configparser are built into Python, no installation needed
